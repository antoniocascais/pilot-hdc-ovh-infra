name: Test Fork Access

on:
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PR from ${{ github.event.pull_request.head.repo.full_name }}"
      - run: echo "Fork? ${{ github.event.pull_request.head.repo.fork }}"
