repos:
  - repo: local
    hooks:
      - id: check-sops-encryption
        name: Check tfvars are SOPS-encrypted
        entry: scripts/check-sops-encryption.sh
        language: script
        files: '\.tfvars$'
        exclude: '\.decrypted\.tfvars$'
