repos:
  - repo: local
    hooks:
      - id: check-sops-encryption
        name: Check secrets are SOPS-encrypted
        entry: scripts/check-sops-encryption.sh
        language: script
        files: '(\.tfvars|ansible/vars/sensitive\.yml)$'
        exclude: '\.decrypted\.(tfvars|yml)$'
